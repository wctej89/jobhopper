%section.search
  .logo
  =form_tag '#', :method => 'get' do
    = text_field_tag :search, params[:search], :placeholder => "Enter keywords here", :class => "home-search", :style => "font-size: 48px; border: none;"
  %ul.search-results

