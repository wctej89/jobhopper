.white_space
  %p stuff to take up the white space
.nav
  -if current_user
    = link_to "Sign Out", signout_path, :class => "btn btn-large btn-danger"
  -else
    = link_to "Sign In", new_session_path, :class => "btn btn-large btn-inverse"
    = link_to "Sign Up", new_user_path, :class => "btn btn-large btn-primary"